Config = {}

-- No visibility key is registered by default. Set a key name such as 'F10'
-- only when the server owner explicitly wants a visibility toggle mapping.
Config.ToggleChatKey = false

-- Reject oversized player chat messages before they reach server hooks/events.
Config.MaxMessageLength = 500
