-- Bluetooth - devices other resources register with the phone: car stereos, boomboxes, headsets,
-- smartwatches. Enabled off leaves the whole system inert, hides the Settings row, and refuses
-- every registration, so nothing can be paired or reached.
return {
    Enabled = true,
}
